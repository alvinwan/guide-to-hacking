```
# Compile and run address book example
clang -framework Foundation -framework AddressBook book.m -o book && ./book

# Compile and run say example
clang -framework Foundation -framework AVFoundation say.m -o say && ./say
```
